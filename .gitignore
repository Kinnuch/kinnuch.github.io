# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock

# Bundler local install (`bundle config set --local path vendor/bundle`)
.bundle/
vendor/

# Python — bytecode and PyInstaller output.
# QuenyaChecker/build/ and dist/ are regenerated by `pyinstaller QuenyaChecker.spec`;
# committing them cost this repo ~37 MB across three copies of the same binary.
__pycache__/
*.py[cod]
build/
dist/
*.manifest

# Editor / OS
.DS_Store
Thumbs.db
*.swp
